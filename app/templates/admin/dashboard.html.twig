{% extends 'base.html.twig' %}

{% block title %}Panneau Administrateur{% endblock %}

{% block body %}
<div class="container">
    <h1>Panneau Administrateur</h1>
    <p class="welcome">Bienvenue {{ app.user.username }}</p>

    <div class="admin-menu">
        <div class="card">
            <h2>👥 Gestion des Utilisateurs</h2>
            <p>Créer, modifier, gérer les utilisateurs et leurs rôles</p>
            <a href="{{ path('app_user_index') }}" class="btn btn-primary">Accéder</a>
        </div>

        <div class="card">
            <h2>✅ Approbations</h2>
            <p>Approuver ou rejeter les posts et commentaires en attente</p>
            <a href="{{ path('app_admin_approvals') }}" class="btn btn-primary">Accéder</a>
        </div>

        <div class="card">
            <h2>📁 Gestion des Catégories</h2>
            <p>Créer, modifier ou supprimer les catégories</p>
            <a href="{{ path('app_category_index') }}" class="btn btn-primary">Accéder</a>
        </div>

        <div class="card">
            <h2>📝 Gestion des Posts</h2>
            <p>Créer, modifier ou supprimer les posts</p>
            <a href="{{ path('app_post_index') }}" class="btn btn-primary">Accéder</a>
        </div>
    </div>

    <div class="back-section">
        <a href="{{ path('app_home') }}" class="btn btn-secondary">Retour à l'accueil</a>
        <a href="{{ path('app_logout') }}" class="btn btn-logout">Déconnexion</a>
    </div>
</div>

<style>
    .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 20px;
        font-family: Arial, sans-serif;
    }

    h1 {
        color: #333;
        text-align: center;
        margin-bottom: 10px;
    }

    .welcome {
        text-align: center;
        color: #666;
        margin-bottom: 30px;
    }

    .admin-menu {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
        gap: 30px;
        margin: 40px 0;
    }

    .card {
        background: #f9f9f9;
        padding: 25px;
        border-radius: 8px;
        box-shadow: 0 2px 8px rgba(0,0,0,0.1);
        border-left: 4px solid #007bff;
    }

    .card h2 {
        margin-top: 0;
        color: #333;
        font-size: 1.3em;
    }

    .card p {
        color: #666;
        margin: 10px 0 20px 0;
    }

    .btn {
        display: inline-block;
        padding: 10px 20px;
        text-decoration: none;
        border-radius: 4px;
        border: none;
        cursor: pointer;
        font-size: 1em;
    }

    .btn-primary {
        background-color: #007bff;
        color: white;
    }

    .btn-secondary {
        background-color: #6c757d;
        color: white;
    }

    .btn-logout {
        background-color: #dc3545;
        color: white;
    }

    .btn:hover {
        opacity: 0.8;
    }

    .back-section {
        text-align: center;
        margin-top: 40px;
        padding-top: 20px;
        border-top: 1px solid #ddd;
    }

    .back-section .btn {
        margin: 10px;
    }

    @media (max-width: 768px) {
        .admin-menu {
            grid-template-columns: 1fr;
        }
    }
</style>
{% endblock %}
